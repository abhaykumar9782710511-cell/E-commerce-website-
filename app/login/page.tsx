'use client'
import {useState} from 'react'
import {useRouter} from 'next/navigation'
import Link from 'next/link'
import {supabase} from '@/lib/supabase'
export default function Login(){const router=useRouter();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setMsg('');setLoading(true);if(!supabase){setMsg('Supabase अभी configure नहीं हुआ है।');setLoading(false);return}const{error}=await supabase.auth.signInWithPassword({email,password});if(error){setMsg(error.message);setLoading(false);return}router.replace('/orders');router.refresh()}return <main className="container section" style={{maxWidth:520}}><Link href="/">← Store</Link><div className="card info" style={{marginTop:18}}><span className="pill">ACCOUNT</span><h1>Welcome back 🙏</h1><p className="muted">Login to track orders and manage your account.</p>{msg&&<p><b>{msg}</b></p>}<form className="form" onSubmit={submit}><div className="field"><label>Email</label><input className="search" style={{width:'100%'}} value={email} onChange={e=>setEmail(e.target.value)} required type="email"/></div><div className="field"><label>Password</label><input className="search" style={{width:'100%'}} value={password} onChange={e=>setPassword(e.target.value)} required type="password"/></div><button className="btn" disabled={loading}>{loading?'Signing in…':'Login'}</button></form><p className="muted">New customer? <Link href="/signup">Create an account</Link></p></div></main>}