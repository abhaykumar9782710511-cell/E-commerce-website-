import {NextResponse} from 'next/server'
import {createClient} from '@supabase/supabase-js'
function db(){return createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!)}
export async function GET(){try{const {data,error}=await db().from('products').select('id,name,slug,description,price,compare_price,sku,stock,image_url,published,category_id,created_at').order('created_at',{ascending:false});if(error)throw error;return NextResponse.json({products:data||[]})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Unable to load products.'},{status:500})}}
export async function POST(req:Request){try{const b=await req.json();if(!b.name||Number(b.price)<0||Number(b.stock)<0)return NextResponse.json({error:'Name, price and stock are required.'},{status:400});const slug=String(b.slug||b.name).toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')+'-'+Date.now().toString().slice(-5);const {data,error}=await db().from('products').insert({name:b.name,slug,description:b.description||'',price:Number(b.price),compare_price:b.compare_price?Number(b.compare_price):null,sku:b.sku||null,stock:Number(b.stock),image_url:b.image_url||null,published:b.published!==false,category_id:b.category_id||null}).select().single();if(error)throw error;return NextResponse.json({product:data},{status:201})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Unable to create product.'},{status:500})}}
export async function PATCH(req:Request){try{const b=await req.json();if(!b.id)return NextResponse.json({error:'Product id is required.'},{status:400});const allowed:any={};for(const k of ['name','description','price','compare_price','sku','stock','image_url','published','category_id'])if(k in b)allowed[k]=b[k];if('price'in allowed)allowed.price=Number(allowed.price);if('stock'in allowed)allowed.stock=Number(allowed.stock);const {data,error}=await db().from('products').update(allowed).eq('id',b.id).select().single();if(error)throw error;return NextResponse.json({product:data})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Unable to update product.'},{status:500})}}
export async function DELETE(req:Request){try{const {id}=await req.json();if(!id)return NextResponse.json({error:'Product id is required.'},{status:400});const {error}=await db().from('products').delete().eq('id',id);if(error)throw error;return NextResponse.json({deleted:true})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Unable to delete product.'},{status:500})}}