'use client'
import {useState} from 'react'
import Link from 'next/link'
export default function NewProduct(){const[saved,setSaved]=useState(false);const[loading,setLoading]=useState(false);const[error,setError]=useState('');const[image,setImage]=useState('');const[name,setName]=useState('');const[category,setCategory]=useState('Krishna Ji Vastra');const[price,setPrice]=useState('');const[mrp,setMrp]=useState('');const[stock,setStock]=useState('');const[sku,setSku]=useState('');const[description,setDescription]=useState('');async function submit(e:any){e.preventDefault();setLoading(true);setError('');try{const r=await fetch('/api/admin/products',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,category,price,mrp,stock,sku,image_url:image,description})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Unable to save product');setSaved(true)}catch(e){setError(e instanceof Error?e.message:'Unable to save product')}finally{setLoading(false)}}if(saved)return <main className="container section"><div className="card info"><span className="pill">SUCCESS</span><h1>Product saved 🎉</h1><p className="muted">Product has been added to the store database.</p><Link className="btn" href="/admin/products">Back to Products</Link></div></main>;return <main className="container section"><div className="sectionhead"><div><span className="pill">ADMIN • PRODUCTS</span><h1>Add New Product</h1></div><Link href="/admin/products">← Products</Link></div>{error&&<div className="card" style={{marginBottom:16}}><b>{error}</b></div>}<div className="admin-layout"><form className="form" onSubmit={submit}><div className="field"><label>Product name</label><input required value={name} onChange={e=>setName(e.target.value)} placeholder="जैसे मोर मुकुट पोशाक"/></div><div className="field"><label>Category</label><select value={category} onChange={e=>setCategory(e.target.value)}><option>Krishna Ji Vastra</option><option>Laddu Gopal</option><option>Shringar Sets</option></select></div><div className="two"><div className="field"><label>Price (₹)</label><input required type="number" min="0" value={price} onChange={e=>setPrice(e.target.value)} placeholder="299"/></div><div className="field"><label>MRP (₹)</label><input type="number" min="0" value={mrp} onChange={e=>setMrp(e.target.value)} placeholder="399"/></div></div><div className="two"><div className="field"><label>Stock</label><input required type="number" min="0" value={stock} onChange={e=>setStock(e.target.value)} placeholder="20"/></div><div className="field"><label>SKU</label><input value={sku} onChange={e=>setSku(e.target.value)} placeholder="SKV-001"/></div></div><div className="field"><label>Product image URL</label><input value={image} onChange={e=>setImage(e.target.value)} placeholder="https://..."/></div><div className="field"><label>Description</label><textarea rows={5} value={description} onChange={e=>setDescription(e.target.value)} placeholder="Product details, fabric, size information..."/></div><button className="btn" type="submit" disabled={loading}>{loading?'Saving…':'Save Product'}</button></form><aside className="card preview"><div className="pic">{image?<img src={image} alt="Preview"/>:'🦚'}</div><div className="info"><span className="pill">PREVIEW</span><h3>{name||'Product preview'}</h3><p className="muted">₹{price||'0'} • Stock {stock||'0'}</p></div></aside></div></main>}